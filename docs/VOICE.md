# 月影とび ボイス台本（栞）

公式ガイドライン（https://vibe.co.jp/luna-occulta/fanworks/assets の「声のお約束」）に基づき、
**公式指定 Voice ID** で制作する。式札かさねの手順は `../shikifuda-kasane/docs/VOICE.md` が正本。

## 話者

**栞（しおり）／道しるべの御霊** — ElevenLabs **Mini `hO2yZ8lxM3axUxL8OeKX`**（公式指定）

`get_spirit` の口調データ:

- 一人称: わたし（めったに使わない）
- 呼びかけ: あるじどの
- 口調: 丁寧で古風、少し気だるげ。「〜ですこと」。人を「頁(ひと)」と数える、書物ずくめの物言い
- 公式の見本: 「第一夜、と。……ゆるりと、まいりましょう」／「……相変わらず、律儀な頁(ひと)ですこと」
- 推奨タグ: `[japanese][calm][whisper][mysterious][low][sexy]`

## 台詞（7本）

**実際に入力した文字列**（読みを固定するため、称号はかな書きにしてある）。

| ファイル名 | 場面 | 入力した台詞 | 尺 |
|---|---|---|---|
| `shiori_start.m4a` | 開始（最初のタップ） | 今宵の道が、開きました。ゆるりと、まいりましょう | 6.5秒 |
| `shiori_r0.m4a` | 結果 0〜9段 | まだ一ページ目、ですこと。次は、もう少し先まで | 5.7秒 |
| `shiori_r1.m4a` | 結果 10〜21段 | よあるき、ですこと。悪くない足どりでした | 4.3秒 |
| `shiori_r2.m4a` | 結果 22〜37段 | つきみの足、ですこと。ずいぶん、遠くまで | 5.0秒 |
| `shiori_r3.m4a` | 結果 38〜49段 | やみよの跳ね手、ですこと。あるじどのも、驚かれましょう | 6.8秒 |
| `shiori_r4.m4a` | **満月成就の儀の最中**（50段に着いた瞬間） | まんげつわたり。つきに、届きましたこと | 5.0秒 |
| `shiori_r5.m4a` | 結果 50段以上 | まんげつわたりの、そのさきへ。あきれるほど、律儀なおかたですこと | 7.1秒 |

すべて先頭に `[japanese][calm][mysterious]` を付けて Eleven v3 で生成し、テイク1を採用した。

結果の台詞は**その回の称号を読み上げる**形にしてある（`src/game.js` の `TITLES` と対応）。
**称号は声と1対1**（5種）。当初は称号10種・声5段階でずらしていたが、「読み上げられた称号と画面の称号が違う」のは気持ちが悪いので、称号の側を声に合わせて5種に減らした。

**r4だけは結果ではなく、満月成就の儀の最中に鳴る**（`moonRite()` から500ms遅らせて＝鐘の一撃を先に聴かせてから重ねる）。

当初は「ちょうど50段で終えた回のリザルト」に置いていたが、**50段に届いた人は普通そのまま続けるので、
実際にはほとんど誰にも聞かれない台詞だった**（2026-08-26 レビュー指摘 → オーナーが実機で
「50段で何も聞こえない」と気づいて確定）。一番の見せ場に置き直した。

結果の声は50段以上をまとめて r5 が受ける。称号は「満月渡り」のまま、台詞で「そのさきへ」と繋ぐ。

**台詞は「どの場面で流れるか」と「その場面の尺」を一緒に設計する。** 儀は1.9秒、r4は5.0秒なので、
声は儀が明けたあとも3秒ほど続く（開始の台詞と同じ作法）。その間は効果音が -7dB に沈む。

## 作り方

ElevenLabs の**無料プランは API からライブラリボイスを使えない**（402 paid_plan_required）ので、
**ブラウザUIから**生成する（式札かさねと同じ。Claude in Chrome での自動操作も可）。

1. ElevenLabs の Text to Speech を開き、**モデルは Eleven v3**
2. 声を **Mini** にする。同名の声があるので、UIの「声のIDをコピー」→ `pbpaste` で
   `hO2yZ8lxM3axUxL8OeKX` と一致することを必ず確認する
3. 台詞の**先頭に推奨タグ**を付けて入力する。例:
   `[japanese][calm][mysterious] 月への道、開きました。……ゆるりと、まいりましょう`
4. 生成 → ダウンロード（mp3）
5. `assets/voice/shiori_<キー>.m4a` として置く（変換は下記）

```bash
# ダウンロードしたmp3を、ゲームで使う形に変換する
ffmpeg -y -i ~/Downloads/<落としたファイル>.mp3 -vn -map 0:a -c:a aac -b:a 96k \
  assets/voice/shiori_start.m4a
```

置いたら `node scripts/build-dist.js && node scripts/build-artifact.js` で取り込まれる。
**音源が無いキーは黙って鳴らないだけ**なので、1本ずつ足していける。

## 音量の設計

`docs/MEDIA.md`（シリーズ共通）の原則どおり **声 > 効果音 > BGM**。

- 声: 1.0（そのまま）
- 効果音: バス `sfxBus` で 0.9。**声が鳴っている間は 0.405（約 -7dB）まで沈める**
- BGM: 0.16。**声が鳴っている間は 0.07 まで下げる**（`duck()` が自動で戻す）

**BGMの経路は2本ある。** Pages版は HTMLAudio（`bgmEl.volume`）、Artifact版は WebAudio（`bgmGain.gain`）。
`bgmGain` だけ下げていた時期があり、**主配信の Pages 版でダッキングが効いていなかった**（2026-08-26 修正）。
`window.__tsukikage.audio` で数値を見れば、聴かなくてもどちらの経路かと沈み具合が分かる。

式札かさねでは「SEが声とほぼ同じ大きさで並んでいた」という取りこぼしがあったので、
ボイスを入れたら**必ず実機で通しで聴く**こと。

## 確認

- イヤホンで、①声だけ浮いていないか ②SEに埋もれていないか ③BGMが戻っているか
- iPhoneでスピーカーから鳴らないときは、まず**マナーモード**を疑う（`docs/MEDIA.md`）


## つまずいた点（次に作るときのために）

### 「……」は「テンテンテン」と読まれる

最初の台詞に三点リーダを入れたら、**そのまま「テンテンテン」と発話された**（8.6秒に膨らんだ）。
Whisperの文字起こしで発覚。**間を置きたいときは句読点で作る**か、v3のタグを使う。三点リーダは書かない。

### 称号はかな書きで読みを固定する

「夜歩き」が **「いえあるき」** と読まれた（文字起こしで検出）。「月への道」も読みが怪しかった。
**画面に出す表記（漢字）と、読ませる文字列（かな）を分けてよい。** 声は かな、画面は漢字でいい。

- 夜歩き → `よあるき`
- 月見の足 → `つきみの足`
- 闇夜の跳ね手 → `やみよの跳ね手`
- 満月渡り → `まんげつわたり`
- 律儀な方 → `律儀なおかた`（「かた」か「ほう」か割れるので、送りをかなにする）
- 一頁目 → `一ページ目`（栞は「頁」を使うキャラだが、読みが不安定なのでカタカナに）

### 生成は30〜40秒かかる

「音声を生成」を押してから2テイクが揃うまで、体感で30〜40秒。**待たずにダウンロードを押すと空振りする**
（波形がグレーのうちは未完成）。自動操作するなら、待ち時間を長めに取るか、波形の色で判定する。

### 後処理は `scripts/prep_voice.py` に集約した

前後の無音を落とし、ピークを-1dBに揃えて m4a にする。台詞ごとの音量差がこれで消える。
v3は末尾に無音が付くことがあり、`r4` は 6.8秒 → 5.0秒 まで削れた。
